---
on:
  push:
    branches:
      - '*'
    tags-ignore:
      - '*'
  pull_request:
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - macos-latest
          - ubuntu-16.04
          - ubuntu-18.04
          - ubuntu-20.04
          - windows-2019
          - windows-2016
        perl: ['5.32', '5.30', '5.28', '5.22']
    name: Perl ${{ matrix.perl }} on ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
      - name: Setup perl
        uses: shogo82148/actions-setup-perl@v2
        with:
          perl-version: ${{ matrix.perl }}
      - run: perl -V
      - name: Install Dependencies
        run: |
          cpanm -iqn --skip-satisfied warnings::unused B::Lint
      - name: Run Tests
        run: |
          perl -MO=Lint NJH/PDFPage.pm
          perl -c -Mwarnings::unused NJH/PDFPage.pm
